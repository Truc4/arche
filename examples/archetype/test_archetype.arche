world GameWorld()

arche Player {
  drag: Float,
  pos: Vec3,
  vel: Vec3,
  health: Float
}

arche Enemy {
  aggression: Float,
  pos: Vec3,
  vel: Vec3
}

sys move(pos, vel) {
  pos = pos + vel;
}

sys dampen(vel, drag) {
  vel = vel * drag;
}

proc initialize() {
  let x = 100;
  let y = 200;
}
