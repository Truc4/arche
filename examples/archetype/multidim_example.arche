arche Message {
  text: char[256],
  author: char[64]
}

arche Matrix {
  data: float[10, 10]
}

proc main() {
  let messages = alloc Message(100);
  let matrices = alloc Matrix(50);

  // Insert some live entries - demonstrates pooling
  // (char[] inserts not yet fully implemented, placeholder)
  // insert(messages, "hello", "alice");
  // insert(matrices, ...);

  let msg = "allocations done\n";
  write(1, msg, msg.length);
}
