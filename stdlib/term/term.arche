raw_enable :: proc() {
  term_raw_enable();
}
raw_restore :: proc() {
  term_raw_restore();
}
read_key :: proc()(k: int) {
  term_read_key()(k);
}

#module
#foreign {
  term_raw_enable  :: proc();
  term_raw_restore :: proc();
  term_read_key    :: proc()(k: int);
}
