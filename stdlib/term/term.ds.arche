// Datasheet for the `term` device. term wraps terminal raw-mode + key reads (termios FFI): no storage
// requirements, no exposed types. Its API + `#foreign` layer live in term.arche. The datasheet's
// presence marks term as a device (importable by name).
