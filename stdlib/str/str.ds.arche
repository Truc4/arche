// Datasheet for the `str` device. str is a pure stateless string-operations library: no storage
// requirements, no exposed types. Its API lives in str.arche, reached via exports. The datasheet's
// presence marks str as a device (importable by name).
