// Datasheet for the `os` device. os wraps syscalls (time, sleep, argv, raw syscall): no storage
// requirements, no exposed types. Its API + `#foreign` layer live in os.arche. The datasheet's
// presence marks os as a device (importable by name).
