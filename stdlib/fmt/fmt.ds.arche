// Datasheet for the `fmt` device. fmt is a pure formatting/printing library: it requires no storage
// and exposes no types — its public API (printf/assert/…) lives in fmt.arche and is reached via the
// export set. The datasheet's presence marks fmt as a device (importable by name).
