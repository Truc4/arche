//! `platform` device — OS-specific conventions (name, path separator, null device). This datasheet's
//! presence marks `platform/` a device; interchangeable `linux/` and `windows/` backends supply the
//! behaviour, and the build target selects which one merges (arche.toml `[target.<name>]`, or
//! `arche build --target <name>`).
// RUN: true
