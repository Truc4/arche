#include "type_ref.h"

/* The TypeRef type tree was deleted in Phase 3 — types are now the interned TypeId
 * (semantic/sem_types.h). type_ref.h now holds only the shared enums + SourceLoc; this
 * translation unit is intentionally empty. */
