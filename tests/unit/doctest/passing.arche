// RUN: %arche test %s

/// Adds two integers.
///
/// # Example
/// ```arche
/// assert(add(2, 3) == 5, "add broken\n");
/// assert(add(10, 1) == 11, "add broken\n");
/// ```
add :: func (a: int, b: int) -> int {
  return a + b;
}
