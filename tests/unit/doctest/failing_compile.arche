// RUN: not %arche test %s

/// The example references a name that does not exist, so it must fail to
/// compile and the doctest run must report failure.
///
/// # Example
/// ```arche
/// assert(nonexistent_fn(1) == 1, "unreachable\n");
/// ```
func real_fn(a: int) -> int {
  return a;
}
