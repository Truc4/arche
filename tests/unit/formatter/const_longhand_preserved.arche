// The explicit-type value-const longhand `name : T : value` is preserved by the formatter
// (the declared type is meaningful and must not be dropped).
// RUN: %arche fmt %s | grep -F "answer : int : 42"
answer: int: 42

main :: proc () {
  printf("%d\n", answer);
}
