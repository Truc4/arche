// The formatter must round-trip the opaque<archetype> phantom tag, not just bare
// opaque (`make format` rewrites every .arche file).
// RUN: %build/arche-fmt %s | grep "opaque<File>"
arche File { fd: opaque }
arche OpenFiles { f: opaque<File> }
