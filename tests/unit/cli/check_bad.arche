// `arche check` rejects a semantic error and exits non-zero.
// RUN: ! %arche check %s 2>%t.err
// RUN: grep -qiF "Undefined symbol" %t.err
entry :: system {
  definitely_not_defined();
}

#run entry
