// `--emit` selects the artifact kind; llvm-ir emits textual IR.
// RUN: %arche build --emit=llvm-ir -o %t.ll %s && grep -q "define" %t.ll
// RUN: %arche build --emit=asm -o %t.s %s && grep -q "\.text\|\.section" %t.s
proc main() { printf("x\n"); }
