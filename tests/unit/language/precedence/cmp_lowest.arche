// Comparisons bind looser than arithmetic: 1 + (2*3) == 7  -> true (1).
// RUN: %arche build -o %t %s && %t | grep "RESULT=1"
#import { fmt }
entry :: system eff {
  fmt.printf("RESULT=%d\n", 1 + 2 * 3 == 7);
}

#run entry
