// Positive: returning an int literal from a func declared `-> int` is clean.
// Runs the program end-to-end to prove the typecheck stays out of valid code's way.
// RUN: %arche build -o %t %s && %t | grep -q "42"
#import { fmt }
answer :: func() -> int {
  return 42;
}

entry :: system {
  x := answer();
  fmt.printf("%d\n", x);
}

#run entry
