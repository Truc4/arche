// A tier-2 subtype is usable AS its backing: a `meters` value flows into a `float` use with no conversion.
// RUN: %arche build -o %t %s && %t | grep "got 5"
#import { fmt }
meters :: float;
entry :: system {
  d: meters = 5.0;
  fmt.printf("got %g\n", d);
}
#run entry
