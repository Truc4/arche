// RUN: %arche -o %t %s && %t

arche Particle {
  pos: float,
  vel: float,
}

proc main() {
  alloc Particle(1000);
  insert(Particle, 1.0, 0.1);
  insert(Particle, 2.0, 0.2);
  insert(Particle, 3.0, 0.3);
  insert(Particle, 4.0, 0.4);
  insert(Particle, 5.0, 0.5);
  insert(Particle, 6.0, 0.6);
  insert(Particle, 7.0, 0.7);
  insert(Particle, 8.0, 0.8);
  Particle.pos = Particle.pos + Particle.vel;
  let p0 = Particle.pos[0];
  let p7 = Particle.pos[7];
  let p0i = p0 * 10.0;
  let p7i = p7 * 10.0;
  assert(p0i == 11, "pos[0] != 1.1\n");
  assert(p7i == 80, "pos[7] != 8.0\n");
}
