// RUN: not %arche build -o %t %s 2>&1 | grep -q "type"
//
// BUG: Compiler should reject explicit float→int assignment
// Expected: Type error
// Current: Silently converts (wrong)
@allow_pure_proc
main :: proc() {
  x := 3.14;
}
