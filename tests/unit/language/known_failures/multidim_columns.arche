// RUN: %arche -o %t %s && %t | grep "allocations done"
arche Message {
  text :: char[256],
  author :: char[64],
}


arche Matrix {
  data :: float[10][10],
}


static table<Message>(100);

static table<Matrix>(50);


proc main() {
  print("allocations done\n");
}
