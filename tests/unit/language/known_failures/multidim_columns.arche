// RUN: %arche build -o %t %s && %t | grep "allocations done"
Message :: arche { text :: char[256], author :: char[64] }

Matrix :: arche { data :: float[10][10] }

Message[100]

Matrix[50]

main :: proc() {
  print("allocations done\n");
}
