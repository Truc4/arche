// RUN: %arche build -o %t %s && %t
#import { fmt }
pos(x, y) :: float
Particle :: arche { pos_x, pos_y }

Particle[5]

main :: proc() {
  insert(Particle, 1.0, 2.0);
  x := Particle.pos_x[0];
  xi := x * 10.0;
  fmt.assert(xi == 10, "x != 1.0\n");
}
