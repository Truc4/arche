// RUN: %arche -o %t %s && %t
pos :: (x: float, y: float)

arche Particle { pos_x, pos_y }

static pool<Particle>(5);

proc main() {
  insert(Particle, 1.0, 2.0);
  let x := Particle.pos_x[0];
  let xi := x * 10.0;
  assert(xi == 10, "x != 1.0\n");
}
