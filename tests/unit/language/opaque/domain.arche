// A foreign resource is a nominal opaque-backed type — here `file :: opaque`. Archetypes can
// hold one as a named component (here `OpenFiles { handle :: file }`). No `opaque<File>` phantom
// tag — `file` is a distinct nominal type that erases to a pointer-width slot. Each opaque type
// is defined exactly once; `file` lives in the `io` module, so reference it qualified as `file`.
// RUN: %arche build -o %t %s && %t | grep "ok"
#import { io fmt }
OpenFiles :: arche { handle :: file }
OpenFiles[8]
main :: proc() {
  fmt.printf("ok opaque_domain\n");
}
