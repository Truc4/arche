// A nominal opaque type in a function signature — a func takes an `io.file` by value (an i64 cell
// at the ABI). `io.file` is distinct in the checker, erased to i64 at codegen.
// RUN: %arche build -o %t %s && %t | grep "ok"
#import { io fmt }
describe :: func(f: io.file) -> int {
  return 0;
}
main :: proc() {
  fmt.printf("ok opaque_param\n");
}
