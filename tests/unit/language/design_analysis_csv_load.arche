// RUN: %arche -o %t %s && %t

proc main() {
  let fd = open("tests/fixtures/transactions.csv", 0);
  let buf: char[256];
  let buf, n = read(fd, buf, 256);
  close(fd);
  assert(n > 0, "read failed\n");
  printf("read %d bytes\n", n);
  printf("CSV multi-assignment test passed\n");
}

