// Negative: a bare call to an `io` export does not resolve — qualified `io.fread(...)` is required.
// RUN: %arche build -o %t %s > %t.out 2>&1 ; grep -q "Undefined symbol 'fread'" %t.out
#import { io }
main :: proc() {
  buf: [16]char;
  fread(0, buf, 16)(buf, _n:);
}
