// Two pool decls resolving to one (imported) canonical shape must error, not silently
// take the first — the driver owns at most one pool per shape.
// RUN: %arche build -o %t %s > %t.out 2>&1 ; grep -qi "already allocated" %t.out
#import { "./phys" }

phys.Particle[8]
phys.Particle[4]
