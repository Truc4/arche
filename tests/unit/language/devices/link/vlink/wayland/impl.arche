// wayland variant of vlink: merged only under ARCHE_SELECT=vlink=wayland. `value` returns 22.
// Not a standalone test.
// RUN: true
#link {
  "m"
}

value :: func() -> Eff(int) {
  return vlink_wval();
}

#module
#foreign {
  vlink_wval :: proc()(r: int);
}
