// `diska` behavior: a pure func that inspects a `file1` handle by borrow (reads nothing — opaque
// cells are not readable in arche; it just returns a constant). No handle is ever created here.
// Not a standalone test.
// RUN: true
tag :: func(h: file1) -> int {
  return 1;
}
