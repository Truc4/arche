// Datasheet for `vcache`, requiring the SAME `key`-less `Cell {a, b}` shape as `vstore`, minimum 16.
// Two devices requiring the same components + shape coalesce (shared vocabulary). The datasheet states
// the component requirements (`a`/`b`) and the storage minimum (`Cell[16]`) only — the `Cell` shape is
// the driver's. Support file.
// RUN: true
a :: float;
b :: float;
[16]Cell;
