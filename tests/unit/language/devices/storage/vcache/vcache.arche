// `vcache` implementation: it defines the same shared `Cell` shape `vstore` defines (they coalesce)
// and a system over it. Support file.
// RUN: true
Cell :: arche { a, b }

vtouch :: map (a) {
  a = a + 1.0;
}
