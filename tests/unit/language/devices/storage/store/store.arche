// The `store` device's implementation: a system over its datasheet's `Node` shape. The device owns
// no storage — the driver sizes the pool (>= the datasheet minimum) and runs this system. Support file.
// RUN: true
bump :: sys (val) {
  val = val + 1.0;
}
