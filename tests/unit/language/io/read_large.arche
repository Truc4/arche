// RUN: %arche -o %t %s && %t
#import io;
proc main() {
  arche_fopen_read("../../../../../tests/fixtures/data.csv")(fd:);
  buf: char[10000];
  arche_fread(fd, buf, 10000)(n:);
  arche_fclose(move fd);
  printf("requested 10000, got: %d\n", n);
}
