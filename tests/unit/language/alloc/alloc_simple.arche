// RUN: %arche build -o %t %s && %t
#import { fmt }
Counter :: arche { val :: int }

[5]Counter(5) {
  val: 7,
}

entry :: system eff {
  fmt.assert(Counter.val[0] == 7, "val[0] not 7\n")();
  fmt.printf("ok\n");
}

#run entry
