// RUN: %arche -o %t %s && %t | grep "done"
arche FloatOps {
  val: float,
}


static table<FloatOps>(8);

proc main() {
  insert(table<FloatOps>, 1.0);
  insert(table<FloatOps>, 2.0);
  print("done\n");
}
