// RUN: %arche build -o %t %s && %t | grep "done"
FloatOps :: arche { val :: float, }

static pool<FloatOps> (8)

main :: proc() {
  insert(FloatOps, 1.0);
  insert(FloatOps, 2.0);
  print("done\n");
}
