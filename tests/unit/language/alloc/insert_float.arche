// RUN: %arche build -o %t %s && %t | grep "done"
#import { fmt }
FloatOps :: arche { val :: float }

[8]FloatOps ?abort;

entry :: system {
  insert(FloatOps { val: 1.0 })(_:, _:);
  insert(FloatOps { val: 2.0 })(_:, _:);
  fmt.print("done\n")(_:);
}

#run entry
