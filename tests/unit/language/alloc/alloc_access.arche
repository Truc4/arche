// RUN: %arche build -o %t %s && %t
#import { fmt }
Point :: arche { x :: int }

Point[5](5) {
  x: 42,
}

main :: proc() {
  fmt.printf("before access\n");
  val := Point.x[0];
  fmt.assert(val == 42, "val not 42\n");
  fmt.printf("ok\n");
}
