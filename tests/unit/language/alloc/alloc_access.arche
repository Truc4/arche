// RUN: %arche build -o %t %s && %t
arche Point {
  x :: int,
}

static pool<Point> (5, 5) {
  x: 42,
}
;

proc main() {
  printf("before access\n");
  val := Point.x[0];
  assert(val == 42, "val not 42\n");
  printf("ok\n");
}
