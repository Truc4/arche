// RUN: %arche build -o %t %s && %t
#import { fmt }
Point :: arche { x :: int }

[5]Point(5) {
  x: 42,
}

entry :: system eff {
  fmt.printf("before access\n");
  val := Point.x[0];
  fmt.assert(val == 42, "val not 42\n")();
  fmt.printf("ok\n");
}

#run entry
