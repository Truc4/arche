// RUN: %arche build -o %t %s && %t
Particle :: arche { pos :: float, }

static pool<Particle> (5)

main :: proc() {
  insert(Particle, 1.0);
  v := Particle.pos[0];
  vi := v * 10.0;
  assert(vi == 10, "value not 1.0\n");
}
