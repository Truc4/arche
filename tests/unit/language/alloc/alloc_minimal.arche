// RUN: %arche -o %t %s && %t
arche Particle {
  pos :: float,
}


static pool<Particle>(5);


proc main() {
  insert(Particle, 1.0);
  v := Particle.pos[0];
  vi := v * 10.0;
  assert(vi == 10, "value not 1.0\n");
}
