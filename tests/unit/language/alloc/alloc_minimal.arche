// RUN: %arche build -o %t %s && %t
#import { fmt }
Particle :: arche { pos :: float }

Particle[5]

main :: proc() {
  insert(Particle, 1.0)(_:, _:);
  v  := Particle.pos[0];
  vi := v * 10.0;
  fmt.assert(vi == 10, "value not 1.0\n");
}
