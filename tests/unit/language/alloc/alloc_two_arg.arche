// RUN: %arche build -o %t %s && %t
Counter :: arche {
  val :: int,
}

static pool<Counter> (10, 3)

main :: proc() {
  assert(Counter.val[0] == 0, "val[0] not 0\n");
  assert(Counter.val[2] == 0, "val[2] not 0\n");
  printf("two-arg alloc ok\n");
}
