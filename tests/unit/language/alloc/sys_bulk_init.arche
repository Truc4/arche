// RUN: %arche build -o %t %s && %t | grep "system_ran"
Task :: arche { val :: int, }

static pool<Task> (1, 1) {
  val: 5,
}

double_val :: sys (val) {
  printf("system_ran\n");
  val = val * 2;
}

main :: proc() {
  run double_val;
  assert(Task.val[0] == 10, "val not 10\n");
}
