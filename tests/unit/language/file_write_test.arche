// RUN: %arche -o %t %s && %t && test -f /tmp/test_output.txt && grep "hello" /tmp/test_output.txt

proc main() {
  let fd := arche_fopen_write("/tmp/test_output.txt");
  arche_fwrite(fd, "hello world\n", 12);
  arche_fclose(fd);
}

