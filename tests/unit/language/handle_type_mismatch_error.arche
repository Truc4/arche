// RUN: %arche -o %t %s > %t.out 2>&1 ; grep -q "type mismatch\|error" %t.out

arche Player {
  pos: float,
}


arche Enemy {
  health: float,
}


static Player(10);

static Enemy(10);


proc main() {
  let p := insert(Player, 1.0);
  delete(Enemy, p);
}
