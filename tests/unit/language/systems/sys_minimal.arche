// RUN: %arche -o %t %s && %t | grep "system_ran"

arche Particle {
  pos: float,
}


static Particle(10);


sys dummy(pos) {
  printf("system_ran\n");
  pos = pos;
}


proc main() {
  insert(Particle, 1.0);
  run dummy;
  printf("pos[0]=%f\n", Particle.pos[0]);
}
