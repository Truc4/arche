// RUN: %arche build -o %t %s && %t | grep "system_ran"
pos :: float
Particle :: arche {
  pos,
}

static pool<Particle> (10);

dummy :: sys(pos) {
  printf("system_ran\n");
  pos = pos;
}

main :: proc() {
  insert(Particle, 1.0);
  run dummy;
  printf("pos[0]=%f\n", Particle.pos[0]);
}
