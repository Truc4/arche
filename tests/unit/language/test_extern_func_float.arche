// RUN: %arche -o %t %s && %t

proc main() {
  let buf: char[4];
  buf[0] = '1';
  buf[1] = '.';
  buf[2] = '5';
  buf[3] = 0;
  let f = atof(buf);
  printf("atof: %g\n", f);
  assert(f == 1.5, "atof failed\n");
}

