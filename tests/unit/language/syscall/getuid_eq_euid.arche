// getuid (102) == geteuid (107) for a normally-launched process.
// RUN: %arche -o %t %s && %t | grep "ok"
unsafe proc main() {
  let uid := syscall(102, 0, 0, 0, 0, 0, 0);
  let euid := syscall(107, 0, 0, 0, 0, 0, 0);
  if (uid == euid) {
    printf("ok\n");
  }
}
