// RUN: %arche -o %t %s && %t | FileCheck %s
// CHECK: done 1000

proc main() {
  let i = 0;
  for (; i < 1000; i += 1) {
    let price_str: char[32];
    let qty_str: char[32];
    let x = i;
    price_str[0] = 65;
  }
  printf("done %d\n", i);
}

