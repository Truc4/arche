// RUN: %arche build -o %t %s && %t
#import { fmt os }
entry :: system eff {
  os.open("/dev/null", 0)(fd:);
  if (fd >= 0) {
    fmt.printf("open succeeded: fd=%d\n", fd);
    os.close(move fd)(_:);
  }
}

#run entry
