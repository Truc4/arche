// RUN: %arche build -o %t %s && %t
#import { fmt os }
entry :: system {
  os.open("/dev/null", 0)(fd:);
  if (fd >= 0) {
    os.close(move fd)(_:);
    fmt.printf("open/close ok\n");
  }
}

#run entry
