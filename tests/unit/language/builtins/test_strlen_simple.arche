// RUN: %arche build -o %t %s && %t
#import { str fmt }
get_len :: func(s: []char) -> int {
  return str.strlen(s);
}

entry :: system eff {
  len := get_len("hello");
  if (len == 5) {
    fmt.printf("strlen ok\n");
  }
}

#run entry
