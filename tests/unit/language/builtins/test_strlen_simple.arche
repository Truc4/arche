// RUN: %arche build -o %t %s && %t
#import { str }
get_len :: func(s: char[]) -> int {
  return str.strlen(s);
}

main :: proc() {
  len := get_len("hello");
  if (len == 5) {
    printf("strlen ok\n");
  }
}
