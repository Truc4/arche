// RUN: %arche build -o %t %s && %t | grep "fd="
// Test if csv_open causes segfault
#import io;

static buf: char[65536];

proc main() {
  io_open("tests/fixtures/data.csv")(fd:);
  printf("fd=%d\n", fd);
  io_close(move fd);
}
