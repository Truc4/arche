// RUN: %arche -o %t %s && %t | grep "fd="
// Test if csv_open causes segfault
use csv;

static buf: char[65536];

proc main() {
  let fd := csv_open("tests/fixtures/data.csv");
  printf("fd=%d\n", fd);
  csv_close(move fd);
}
