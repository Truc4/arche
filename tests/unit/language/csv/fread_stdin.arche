// RUN: %arche build -o %t %s && %t
#import { io fmt }
main :: proc() {
  buf: [256]char;
  io.stdin()(fd:);
  io.fread(fd, 256)(buf, n:);
  fmt.printf("read from stdin returned %d bytes\n", n);
  fmt.printf("multi-assignment with read() works\n");
  io.fclose(move fd)(_:);
}
