// RUN: %arche -o %t %s && %t
proc main() {
  let buf: char[256];
  let fd := arche_stdin();
  let n := arche_fread(fd, buf, 256);
  printf("read from stdin returned %d bytes\n", n);
  printf("multi-assignment with read() works\n");
}
