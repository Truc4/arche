// RUN: %arche build -o %t %s && %t
#import { io fmt }

buf: char[65536]

main :: proc() {
  fmt.printf("about to open file\n");
  io_open("tests/fixtures/data.csv")(fd:);
  fmt.printf("fd=%d\n", fd);
  io_close(move fd);
}
