// RUN: %arche -o %t %s && %t
#import io;

static buf: char[65536];

proc main() {
  printf("about to open file\n");
  io_open("tests/fixtures/data.csv")(fd:);
  printf("fd=%d\n", fd);
  io_close(move fd);
}
