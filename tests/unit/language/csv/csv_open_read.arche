// RUN: %arche -o %t %s && %t
use csv;

static buf: char[65536];

proc main() {
  printf("about to open file\n");
  fd := csv_open("tests/fixtures/data.csv");
  printf("fd=%d\n", fd);
  csv_close(move fd);
}
