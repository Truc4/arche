// RUN: %arche -o %t %s && %t
use io;

static buf: char[65536];

proc main() {
  printf("about to open file\n");
  fd := io_open("tests/fixtures/data.csv");
  printf("fd=%d\n", fd);
  io_close(move fd);
}
