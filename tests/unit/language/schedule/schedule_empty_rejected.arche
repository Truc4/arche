// An empty `#schedule { }` lists no work and is rejected — a schedule must name at least one
// system/map.
// RUN: %arche build -o %t %s > %t.out 2>&1 ; grep -qi "empty .#schedule" %t.out
v :: int;
Counter :: arche { v }
[1]Counter;

#schedule { }

main :: proc() {
  tick();
}
