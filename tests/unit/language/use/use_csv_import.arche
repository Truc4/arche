// RUN: %arche -o %t %s && %t
#import io;

proc main() {
  io_open("tests/fixtures/data.csv")(fd:);
  if (fd) {
    buf: char[65536];
    io_read_chunk(fd, buf, 65536)(buf, n:);
  }
  io_close(move fd);
}
