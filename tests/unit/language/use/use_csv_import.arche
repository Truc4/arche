// RUN: %arche -o %t %s && %t
use csv;

proc main() {
  fd := csv_open("tests/fixtures/data.csv");
  if (fd) {
    buf: char[65536];
    buf, n := csv_read_chunk(fd, move buf, 65536);
  }
  csv_close(move fd);
}
