// RUN: %arche -o %t %s && %t
use io;

proc main() {
  fd := io_open("tests/fixtures/data.csv");
  if (fd) {
    buf: char[65536];
    buf, n := io_read_chunk(fd, move buf, 65536);
  }
  io_close(move fd);
}
