// RUN: %arche build -o %t %s && %t
#import { io }

main :: proc() {
  io.open("tests/fixtures/data.csv")(fd:);
  if (fd) {
    buf: char[65536];
    io.read_chunk(fd, buf, 65536)(buf, n:);
  }
  io.close(move fd);
}
