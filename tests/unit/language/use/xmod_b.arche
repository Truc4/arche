// Support module for cross_module.arche (loaded via `#import { xmod_b }`), not a
// standalone test. It calls xmod_a's `helper` via qualified access (`xmod_a.helper`) —
// bare cross-module names no longer resolve.
// RUN: true
useit :: func(x: int) -> int {
  return xmod_a.helper(x);
}
