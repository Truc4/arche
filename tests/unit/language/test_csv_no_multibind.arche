// RUN: %arche -o %t %s && %t | grep "n="

use csv;

static buf: char[65536];

proc main() {
  let fd := csv_open("/home/curt/Code/arche/tests/fixtures/data.csv");
  if (fd > 0) {
    let n := csv_read_chunk(fd, buf, 65536);
    printf("n=%d\n", n);
  }
}
