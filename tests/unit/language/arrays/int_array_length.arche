// .length on an int array returns N (regression for the bounded-array length).
// RUN: %arche build -o %t %s && %t | grep -q "len=8"
#import { fmt }
main :: proc() {
  b: [8]int;
  fmt.printf("len=%d\n", b.length);
}