// Out-of-bounds char[N] write is rejected at compile time (full totality: the index must be provable).
// RUN: %arche build -o %t %s > %t.out 2>&1 ; grep -qi "may panic" %t.out
main :: proc() {
  b: char[4];
  i    := 9;
  b[i] = 65;
}
