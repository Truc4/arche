// Out-of-bounds char[N] write aborts at runtime: the proc default policy is `!abort`, so an
// unguarded OOB index hits the bounds check and aborts the program.
// RUN: %arche build -o %t %s && ! %t
main :: proc() {
  b: char[4];
  i    := 9;
  b[i] = 65;
}
