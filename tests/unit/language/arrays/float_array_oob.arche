// Out-of-bounds float-array access aborts at runtime: the proc default policy is `!abort`, so an
// unguarded OOB index hits the bounds check and aborts the program.
// RUN: %arche build -o %t %s && ! %t
#import { fmt }
main :: proc() {
  b: [4]float;
  i := 9;
  fmt.printf("%g\n", b[i]);
}