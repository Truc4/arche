// Out-of-bounds i64-array write aborts at runtime: the proc default policy is `!abort`, so an
// unguarded OOB index hits the bounds check and aborts the program.
// RUN: %arche build -o %t %s && ! %t
entry :: system {
  a: [4]i64;
  i := 7;
  a[i] = 1;
}

#run entry
