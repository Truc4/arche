// RUN: %arche -o %t %s > %t.out 2>&1 ; grep -qE "Error.*bounds|count.*0" %t.out
// Uninitialized alloc has count=0, accessing any row should error

arche Counter {
  val: int,
}

static Counter(5);



proc main() {
  let x = Counter.val[0];
  printf("should not reach\n");
}

