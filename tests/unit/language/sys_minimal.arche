// RUN: %arche -o %t %s && %t | grep 1

arche Particle {
  pos: float,
}

static Particle(10);



sys dummy(pos) {
  pos = pos;
}



proc main() {
  insert(Particle, 1.0);
  run dummy;
  print(Particle.pos[0]);
  print("\n");
}

