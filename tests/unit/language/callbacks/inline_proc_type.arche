// A callback param can be an inline proc/func signature, not just a named alias.
// RUN: %arche build -o %t %s && %t | grep -c "hi" | grep "2"
twice :: proc ()() {
  printf("hi\n");
}

go :: proc (f: proc ()())() {
  f()();
  f()();
}

main :: proc () {
  go(twice)();
}
