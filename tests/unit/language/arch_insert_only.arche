// RUN: %arche -o %t %s && %t

arche Particle {
  pos: float,
  vel: float,
}

static Particle(5);



proc main() {
  insert(Particle, 1.0, 0.1);
  let p = Particle.pos[0];
  let pi = p * 10.0;
  assert(pi == 10, "pos != 1.0\n");
}

