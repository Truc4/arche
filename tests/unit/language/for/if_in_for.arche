// RUN: %arche build -o %t %s && %t
#import { fmt }
main :: proc() {
  for (i := 0; i < 3; i += 1) {
    fmt.printf("before if\n");
    if (1) {
      fmt.printf("in if\n");
    }
  }
  fmt.printf("ok\n");
}
