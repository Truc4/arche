// RUN: %arche build -o %t %s && %t
// Step 5: Break statement with infinite for loop
#import { fmt }
main :: proc() {
  i := 0;
  for {
    if (i == 2) {
      break;
    }
    fmt.printf("iteration\n");
    i = i + 1;
  }
  fmt.printf("break ok\n");
}
