// A local value constant `k :: e` inside a proc body — the inferred-type constant form.
// RUN: %arche build -o %t %s && %t | grep "k: 10"
#import { fmt }
entry :: system {
  k :: 10;
  fmt.printf("k: %d\n", k);
}

#run entry
