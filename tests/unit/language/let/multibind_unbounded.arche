// RUN: %arche -o %t %s && %t | grep "result: 99"
// Unbounded char[] buffer filled in place, scalar count returned.
func fill_buf(buf: char[]) -> int {
  sprintf(buf, "hello");
  return 99;
}

proc main() {
  buf: char[32];
  result := fill_buf(buf);
  printf("result: %d\n", result);
}
