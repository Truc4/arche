// RUN: %arche build -o %t %s && %t
pair :: proc()(x: int, y: int) {
  x = 42;
  y = 7;
}

main :: proc() {
  pair()(x:, y:);
  assert(x == 42, "x not 42\n");
  assert(y == 7, "y not 7\n");
  printf("multivalue test ok\n");
}
