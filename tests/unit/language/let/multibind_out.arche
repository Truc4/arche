// RUN: %arche -o %t %s && %t | grep "result: 99"
// A function fills the caller's buffer in place and returns a scalar count.
func fill_buf(buf: char[32]) -> int {
  sprintf(buf, "hello");
  return 99;
}

proc main() {
  buf: char[32];
  result := fill_buf(buf);
  printf("result: %d\n", result);
}
