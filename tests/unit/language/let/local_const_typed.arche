// A local value constant with an explicit type: `k : int : e` (the longhand of `k :: e`).
// RUN: %arche build -o %t %s && %t | grep "k: 42"
#import { fmt }
entry :: system {
  k: int: 42;
  fmt.printf("k: %d\n", k);
}

#run entry
