// A local constant is immutable: assigning to a `::` binding is a compile-time error.
// RUN: %arche build -o %t %s > %t.out 2>&1 ; grep -qi "cannot assign to constant" %t.out
main :: proc() {
  k :: 1;
  k = 2;
  printf("unreachable\n");
}
