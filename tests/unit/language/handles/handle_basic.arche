// RUN: %arche build -o %t %s && %t
Player :: arche { pos :: float }

AliveList :: arche { player_ref :: handle<Player> }

Player[10]

AliveList[10]

main :: proc() {
  insert(Player, 1.0)(p_handle:, _:);
  insert(AliveList, p_handle)(_:, _:);
}
