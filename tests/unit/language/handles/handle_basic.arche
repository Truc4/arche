// RUN: %arche -o %t %s && %t
arche Player {
  pos :: float,
}


arche AliveList {
  player_ref :: handle<Player>,
}


static pool<Player>(10);

static pool<AliveList>(10);


proc main() {
  p_handle := insert(Player, 1.0);
  insert(AliveList, p_handle);
}
