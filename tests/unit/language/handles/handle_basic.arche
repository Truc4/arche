// RUN: %arche -o %t %s && %t
arche Player {
  pos: float,
}


arche AliveList {
  player_ref: handle<Player>,
}


static table<Player>(10);

static table<AliveList>(10);


proc main() {
  let p_handle := insert(table<Player>, 1.0);
  insert(table<AliveList>, p_handle);
}
