// RUN: %arche build -o %t %s && %t | grep "slot"
#import { fmt }
Player :: arche { pos :: float }

[10]Player ?abort;

entry :: system eff {
  insert(Player { pos: 1.0 })(h:, _:);
  fmt.print("slot 0 handle created")(_:);
}

#run entry
