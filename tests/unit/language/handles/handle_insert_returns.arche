// RUN: %arche build -o %t %s && %t | grep "slot"
#import { fmt }
Player :: arche { pos :: float }

Player[10]

main :: proc() {
  insert(Player, 1.0)(h:, _:);
  fmt.print("slot 0 handle created");
}
