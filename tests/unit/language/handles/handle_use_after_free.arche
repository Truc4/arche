// RUN: %arche -o %t %s && ! %t
arche Player {
  pos: float,
}


static table<Player>(10);


proc main() {
  let h := insert(table<Player>, 1.0);
  delete(table<Player>, h);
  delete(table<Player>, h);
}
