// RUN: %arche -o %t %s && ! %t
arche Player {
  pos: float,
}


static table<Player>(10);


proc main() {
  let h1 := insert(table<Player>, 1.0);
  delete(table<Player>, h1);
  let h2 := insert(table<Player>, 2.0);
  delete(table<Player>, h1);
}
