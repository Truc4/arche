// RUN: %arche -o %t %s && %t
arche Particle {
  pos :: float,
  vel :: float,
}


static pool<Particle>(5);


proc main() {
  insert(Particle, 1.0, 0.1);
  p := Particle.pos[0];
  pi := p * 10.0;
  assert(pi == 10, "pos != 1.0\n");
}
