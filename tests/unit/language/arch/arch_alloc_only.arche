// RUN: %arche build -o %t %s && %t
arche Particle {
  pos :: float,
  vel :: float,
}

static pool<Particle> (5);

proc main() {
  insert(Particle, 5.5, 0.1);
  p := Particle.pos[0];
  pi := p * 10.0;
  assert(pi == 55, "pos != 5.5\n");
}
