// RUN: %arche build -o %t %s && %t | grep "allocations done"
#import { fmt }
Message :: arche { text :: [256]char, author :: [64]char }

Matrix :: arche { data :: [10][10]float }

[100]Message;

[50]Matrix;

main :: proc() {
  fmt.print("allocations done\n");
}