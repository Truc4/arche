// RUN: %arche build -o %t %s && %t | grep "INT=7 FLOAT=2.5"
func pick_int(x: int) -> int {
  return x + 5;
}
func pick_float(x: float) -> float {
  return x + 1.5;
}

func pick = {
  pick_int, pick_float
}
;

proc main() {
  a := pick(2);
  b := pick(1.0);
  printf("INT=%d FLOAT=%g\n", a, b);
}
