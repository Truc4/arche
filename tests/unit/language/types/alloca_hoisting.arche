// RUN: %arche build -o %t %s && %t | grep "done 1000"
#import { fmt }
main :: proc() {
  i := 0;
  for (; i < 1000; i += 1) {
    price_str : [32]char;
    qty_str   : [32]char;
    x := i;
    price_str[0] = 65;
  }
  fmt.printf("done %d\n", i);
}
