// RUN: %arche build -o %t %s && %t | grep "done 1000"
main :: proc() {
  i := 0;
  for (; i < 1000; i += 1) {
    price_str: char[32];
    qty_str: char[32];
    x := i;
    price_str[0] = 65;
  }
  printf("done %d\n", i);
}
