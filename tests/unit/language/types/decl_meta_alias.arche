// `foo : type : T` is the explicit meta-type longhand for a nominal type alias — equivalent to
// the inferred `foo :: T`. Here `weight` backs `int`.
// RUN: %arche -o %t %s && %t | grep "ok: 7"
weight : type : int

proc main() {
  w: weight = 7;
  printf("ok: %d\n", w);
}
