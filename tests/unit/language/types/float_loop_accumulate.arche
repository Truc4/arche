// RUN: %arche -o %t %s && %t
proc main() {
  total := 0.0;
  i := 0;
  for (;i < 4;) {
    v := 0.5;
    total = total + v;
    i = i + 1;
  }
  assert(total == 2.0, "float loop accumulate failed\n");
}
