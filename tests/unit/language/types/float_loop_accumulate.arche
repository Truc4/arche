// RUN: %arche build -o %t %s && %t
#import { fmt }
entry :: system {
  total := 0.0;
  i     := 0;
  for (; i < 4;) {
    v := 0.5;
    total = total + v;
    i     = i + 1;
  }
  fmt.assert(total == 2.0, "float loop accumulate failed\n");
}

#run entry
