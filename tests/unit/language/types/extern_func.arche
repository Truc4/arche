// RUN: %arche build -o %t %s && %t
#import { parse fmt }
entry :: system {
  buf: [4]char;
  buf[0] = '4';
  buf[1] = '2';
  buf[2] = 0;
  n := parse.atoi(buf);
  fmt.printf("atoi: %d\n", n);
  fmt.assert(n == 42, "atoi failed\n");
}

#run entry
