// RUN: %arche build -o %t %s && %t
#import { parse }
main :: proc() {
  buf: char[4];
  buf[0] = '4';
  buf[1] = '2';
  buf[2] = 0;
  n := parse.atoi(buf);
  printf("atoi: %d\n", n);
  assert(n == 42, "atoi failed\n");
}
