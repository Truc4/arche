// RUN: %arche build -o %t %s && %t
#import { parse fmt }
main :: proc() {
  buf: [4]char;
  buf[0] = '1';
  buf[1] = '.';
  buf[2] = '5';
  buf[3] = 0;
  f := parse.atof(buf);
  fmt.printf("atof: %g\n", f);
  fmt.assert(f == 1.5, "atof failed\n");
}
