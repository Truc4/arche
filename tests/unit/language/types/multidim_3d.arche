// RUN: %arche build -o %t %s && %t | grep "allocations done"
#import { fmt }
Tensor :: arche { data :: [5][5]float }

[5]Tensor;

entry :: system eff {
  fmt.print("allocations done\n")(_:);
}

#run entry
