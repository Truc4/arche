// A value const written with the explicit-type longhand `name : T : value`.
// RUN: %arche build -o %t %s && %t | grep "v: 42"
#import { fmt }
answer: int: 42
main :: proc() {
  fmt.printf("v: %d\n", answer);
}
