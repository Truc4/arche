// RUN: %arche -o %t %s && %t
func test_fill_buf(buf: char[32]) -> (char[], int) {
  return buf, 5;
}


proc main() {
  buf, n := test_fill_buf(buf);
  assert(n > 0, "test_fill_buf returned <= 0\n");
  printf("out param test ok\n");
}
