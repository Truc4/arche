// RUN: %arche build --exported-mutable=allow -o %t %s && %t | grep ABC
#import { fmt }
buf: [128]char;

entry :: system eff {
  buf[0] = 65;
  buf[1] = 66;
  buf[2] = 67;
  fmt.printf("%c%c%c\n", buf[0], buf[1], buf[2]);
}

#run entry
