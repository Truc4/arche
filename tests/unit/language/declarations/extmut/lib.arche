// A plain library module with a mutable global on its exported surface. Imported by the
// exported_mutable_* tests, where W0022 flags `count`. Not a standalone test — only imported.
// RUN: true
count := 0;

bump :: system {
  count = count + 1;
}
