// RUN: %arche build -o %t %s && %t
test_strlen :: func (s: char[]) -> int {
  return strlen(s);
}

main :: proc () {
  n := test_strlen("hello");
  assert(n == 5, "strlen failed\n");
  printf("ok\n");
}
