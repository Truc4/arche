// RUN: %arche build -o %t %s && %t
#import { str fmt }
test_strlen :: func(s: []char) -> int {
  return str.strlen(s);
}

entry :: system {
  n := test_strlen("hello");
  fmt.assert(n == 5, "strlen failed\n")();
  fmt.printf("ok\n");
}

#run entry
