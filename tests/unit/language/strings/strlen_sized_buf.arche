// RUN: %arche build -o %t %s && %t
func check_buf(buf: char[32]) -> int {
  return strlen(buf);
}

proc main() {
  n1 := strlen("hello");
  printf("direct strlen: %d\n", n1);
  n2 := check_buf("hello");
  printf("function strlen: %d\n", n2);
}
