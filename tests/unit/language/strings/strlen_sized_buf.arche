// RUN: %arche build -o %t %s && %t
check_buf :: func (buf: char[32]) -> int {
  return strlen(buf);
}

main :: proc () {
  n1 := strlen("hello");
  printf("direct strlen: %d\n", n1);
  n2 := check_buf("hello");
  printf("function strlen: %d\n", n2);
}
