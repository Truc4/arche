// RUN: %arche build -o %t %s && %t
#import { str }
test_strlen :: func(s: char[]) -> int {
  return str.strlen(s);
}

main :: proc() {
  n := test_strlen("hello");
  printf("strlen returned: %d\n", n);
}
