// RUN: %arche -o %t %s && %t

arche Particle {
  pos: float,
}



proc main() {
  alloc Particle(10);
  insert(Particle, 1.0);
  let v = Particle.pos[0];
  let vi = v * 10.0;
  assert(vi == 10, "value not 1.0\n");
}

