arche Body {
  pos: float
}

sys dummy(pos) {
  pos = pos;
}

proc main() {
  let b = alloc Body(100);
  insert(b, 1.0);
  insert(b, 2.0);
  insert(b, 3.0);
  run dummy in (null);
  let msg = "ok\n";
  print(msg);
}

