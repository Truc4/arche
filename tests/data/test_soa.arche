arche Particle {
  pos: float,
  vel: float
}

sys move(pos, vel) {
  pos = pos + vel;
}

proc main() {
  let p = Particle.alloc((null): 1000);
  p.pos = p.pos + p.vel;
  let msg = "test passed\n";
  print(msg);
}

