arche Particle {
  pos: float,
  vel: float
}

sys move(pos, vel) {
  pos = pos + vel;
}

proc main() {
  let p = alloc Particle(1000);
  p.pos = p.pos + p.vel;
  run move;
  let msg = "test passed\n";
  write(1, msg, msg.length);
}
