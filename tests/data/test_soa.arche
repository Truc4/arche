arche Entity {
  pos: Float,
  vel: Float
}

sys update(pos, vel) {
  for i in ? {
    pos[i] = pos[i] + vel[i];
  }
}

proc init() {
  let e = Entity.alloc((null): 0);
  insert(e, 1.0, 0.5);
  delete(e, 0);
}

