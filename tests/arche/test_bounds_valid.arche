arche Particle {
  id: int,
}

proc main() {
  alloc Particle(5);
  insert(Particle, 1);
  insert(Particle, 2);
  let a = Particle.id[0];
  let b = Particle.id[1];
  assert(a == 1, "bounds_valid: a != 1\n");
  assert(b == 2, "bounds_valid: b != 2\n");
}

