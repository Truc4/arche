arche Particle {
  id: int,
}

proc main() {
  alloc Particle(5);
  insert(Particle, 1);
  insert(Particle, 2);
  let a = Particle.id[0];
  let b = Particle.id[1];
  assert(a == 1, "a != 1\n");
  assert(b == 2, "b != 2\n");
}

