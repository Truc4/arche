arche Particle {
  id: int
}

proc main() {
  let particles = alloc Particle(5);
  insert(particles, 1);
  insert(particles, 2);
  insert(particles, 3);
  for i in Particle {
    let x = particles.id[i];
  }
}

