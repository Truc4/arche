arche Body {
  pos: float,
}

sys dummy(pos) {
  pos = pos;
}

proc main() {
  alloc Body(100);
  insert(Body, 1.0);
  insert(Body, 2.0);
  insert(Body, 3.0);
  run dummy;
  let msg = "ok\n";
  print(msg);
}

